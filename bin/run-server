#!/bin/bash
set -e

# Run standard Django preparation
python manage.py collectstatic --no-input

# Start Granian with ASGI interface
exec granian \
    --interface asgi \
    --host 0.0.0.0 \
    --port 8000 \
    --workers 4 \
    --backlog 1024 \
    finis.asgi:application
